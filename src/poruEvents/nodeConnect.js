module.exports.run = (client, node) => {
  console.log(`[NODE] ${node.name} a été télécharger avec succès`.blue);
  console.log(`[Support] go join discord.gg/novaworld`.yellow);
  console.log("----------------------------------------".white);
};